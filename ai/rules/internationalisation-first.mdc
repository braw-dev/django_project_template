---
description: When writing strings in templates, views or models make sure to go i18n first
alwaysApply: false
---

# Internationalization First (i18n)

Apply the canonical guide in `../../ai/docs/internationalisation-first.md`.

Use this rule as an index only. The shared prose lives in `ai/docs/` so Claude, Cursor, and other agents read the same source text.
