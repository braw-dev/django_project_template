---
description: Django stack conventions and architecture for this project
globs: "**/*.py"
alwaysApply: false
---

# Django Stack

Apply the canonical guide in `../../ai/docs/django-stack.md`.

Use this rule as an index only. The shared prose lives in `ai/docs/` so Claude, Cursor, and other agents read the same source text.
