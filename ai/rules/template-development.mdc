---
description: Guidelines for developing on the Django Project Template itself (not a generated project)
globs: project_name/**/*.py-tpl,dev/**/*,AGENTS.md
alwaysApply: false
---

# Template Development Rules

Apply the canonical guide in `../../ai/docs/template-development.md`.

Use this rule as an index only. The shared prose lives in `ai/docs/` so Claude, Cursor, and other agents read the same source text.
