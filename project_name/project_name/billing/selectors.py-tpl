"""Data retrieval functions for billing."""

from typing import Any, cast

from django.db.models import Q
from django.utils import timezone

from {{ project_name }}.tenancy import models as tenancy_models

from . import models

# TODO: Remove this workaround when ty supports Django's dynamic model attributes.
SubscriptionModel = cast(Any, models.Subscription)


def has_active_subscription(team: tenancy_models.Team) -> bool:
    """Check if team has an active subscription.

    A subscription is considered active if:
    - Status is 'active' or 'trialing'
    - current_period_end is in the future (or None)

    Args:
        team: Team instance to check

    Returns:
        True if team has an active subscription, False otherwise
    """
    now = timezone.now()
    return SubscriptionModel.all_objects.filter(
        team=team,
        status__in=[
            models.Subscription.SubscriptionStatus.ACTIVE,
            models.Subscription.SubscriptionStatus.TRIALING,
        ],
    ).filter(
        Q(current_period_end__gt=now) | Q(current_period_end__isnull=True)
    ).exists()


def get_active_subscription(
    team: tenancy_models.Team,
) -> models.Subscription | None:
    """Get the most recent active subscription for a team.

    Args:
        team: Team instance

    Returns:
        Most recent active Subscription, or None if none exists
    """
    now = timezone.now()
    return (
        SubscriptionModel.all_objects.filter(
            team=team,
            status__in=[
                models.Subscription.SubscriptionStatus.ACTIVE,
                models.Subscription.SubscriptionStatus.TRIALING,
            ],
        )
        .filter(
            Q(current_period_end__gt=now) | Q(current_period_end__isnull=True)
        )
        .order_by("-created_at")
        .first()
    )


def get_subscription_by_polar_id(polar_id: str) -> models.Subscription | None:
    """Get a subscription by Polar subscription ID.

    Args:
        polar_id: Polar subscription ID

    Returns:
        Subscription instance, or None if not found
    """
    try:
        return SubscriptionModel.all_objects.get(polar_subscription_id=polar_id)
    except SubscriptionModel.DoesNotExist:
        return None

